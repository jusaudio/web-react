import * as React from 'react';
// import styled from "styled-components"

interface IPrimaryButtonProps {
  label: string;
}
export const UserSearch = ({ label }: IPrimaryButtonProps) =>
    <div>
        { label }
    </div>
